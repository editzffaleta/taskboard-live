# Skills arquivadas

Skills fora do fluxo padrão do template, preservadas para consulta/projetos legados.
O Claude Code não as carrega daqui (só carrega subpastas diretas com `SKILL.md` válido? —
na dúvida, o prefixo `_` e este README deixam a intenção explícita: **não usar em projetos novos**).

- **deploy-node-ubuntu-vps** — deploy manual em VPS Ubuntu (PM2 + Caddy + PostgreSQL + ufw).
  Substituída pela **`deploy-dokploy`** (painel Dokploy com Traefik/Compose/backups). Use esta
  arquivada apenas para manter projetos que já rodam na arquitetura antiga.
