#!/usr/bin/env bash
# Roda o gate antes do push (mesmo conjunto de checks do CI). Sem dependências.
set -euo pipefail

if [ -f scripts/ci/gate.sh ]; then
  echo "→ pre-push: rodando gate…"
  bash scripts/ci/gate.sh
else
  echo "→ pre-push: scripts/ci/gate.sh não encontrado, pulando."
fi
