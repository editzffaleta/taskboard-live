# Execution Log

Histórico de execução das mudanças (uma linha por mudança): data, commit, observações.

| Mudança | Data | Commit | Observações |
| --- | --- | --- | --- |
| 001-base-do-projeto | 2026-07-05 | d511c2d (#6) | Base técnica: Prisma (DbModule/PrismaService, schema modular, seed neutro, docker-compose), pacote @taskboard/shared (96 testes), base Nest de erros+JWT; frontend shell (public)/(private), i18n pt/en, branding TaskBoard Live. Gate/CI verde. Frições do scaffold resolvidas (multer override, @repo→@taskboard, Next 16 lockfile, postinstall prisma generate, turbo ^build, semgrep pin/cooldown/.semgrepignore). Capability base-projeto (corrigido nome de pasta que nascera fundacao-e2e). |
| 002-design-system-shell | 2026-07-05 | #11 | Design system: paleta TaskBoard Live (azul #2563EB/#1D4ED8) claro/escuro, toggle de tema persistente, tipografia Inter/JetBrains Mono, shell (logo, sidebar Quadros/Conta, toaster theme-aware), i18n pt/en. Gate/CI verde. Capability design-system. |
| 003-registro-usuario | 2026-07-05 | #12 | Registro: módulo auth (agregado user name/email/password, single-tenant), bcrypt, register-user (unicidade global, 100% cobertura, 24 testes), model Prisma User + migration, POST /auth/register (201/409/422), i18n; tela /join (cadastro integrado, login visual). Fix: modules/auth passou a declarar @taskboard/shared (ordem topológica do turbo ^build). Gate/CI verde. Capability registro-usuario. |
| 004-login-sessao | 2026-07-05 | #13 | Login/sessão: login-user (401 credenciais inválidas, 100% cobertura, 30 testes), jwt.util { sub, name, email }, POST /auth/login → { token, user }; frontend AuthContext/AuthGuard, decodeJwtPayload UTF-8, cookie auth_token, proteção de (private), redirects, logout. Validado por Playwright. Gate/CI verde. Capability login-sessao. |
| 005-quadros-crud | 2026-07-05 | #14 | Quadros: módulo board (agregados board+membership), create-board (Board+owner atômico), rename/delete (owner), list-my-boards, get-board (membro); Prisma Board+BoardMember (unique+cascade)+migrations; endpoints /boards. 43 testes 100%. Frontend: dashboard, criar/renomear/excluir, /boards/[id] placeholder, i18n. Gate/CI verde. Capability quadros. |
| 006-tempo-real-gateway | 2026-07-05 | #15 | Tempo real: Socket.IO no NestJS, BoardGateway (handshake JWT, salas board:{id}, board:join autorizado por BoardMember, presença presence.update), porta RealtimeEmitter (emitToBoard) exportada pelo BoardModule para 007/008/010/011. Testes de integração socket.io-client real. Gate/CI verde. Capability tempo-real. |
| 007-listas-colunas | 2026-07-05 | #16 | Listas (colunas): agregado list no módulo board, create/rename/delete/move (renormaliza posições), 57 testes; endpoints /boards/:boardId/lists (guard membership), Prisma List (FK cascade)+migration; emite list.* via RealtimeEmitter; i18n. Gate/CI verde. Capability listas. |
| 008-cartoes | 2026-07-05 | #17 | Cartões: agregado card no módulo board, create/edit/delete/move (toListId+position, renormaliza origem/destino), 76 testes; endpoints /boards/:boardId/cards (guard membership), Prisma Card (FK cascade)+migration; emite card.* (card.moved {cardId,fromListId,toListId,position}) via RealtimeEmitter; i18n. Backend do kanban completo. Gate/CI verde. Capability cartoes. |
| 009-quadro-ao-vivo-ui | 2026-07-05 | #18 | VITRINE — quadro ao vivo: página /boards/[id] com DnD (@hello-pangea/dnd), update otimista+reversão, useBoardSocket (handshake JWT, board:join, aplica eventos ao vivo + reconcilia), presença, CRUD inline. Backend: GET /boards/:id enriquecido (lists+cards aninhados ordenados, get-board-detail) fechando gap de leitura. 80 testes módulo board. Tempo real validado com 2 clientes socket reais. Gate/CI verde. Capability quadro-ao-vivo. |
| 010-compartilhamento-membros | 2026-07-05 | #19 | Membros: add-member (por e-mail, só owner, porta MemberDirectory), remove-member, list-members; /boards/:boardId/members (GET/POST/DELETE), emite member.added; 92 testes módulo board. Frontend: painel "Compartilhar" (listar/convidar/remover, só owner) com atualização ao vivo. Gate/CI verde. Capability membros. |
| 011-atividade-do-quadro | 2026-07-05 | #20 | Atividade: agregado activity + ActivityRecorder (persiste + emite activity.created, isolado), list-activity paginado, GET /boards/:boardId/activity, Prisma Activity+migration; ganchos nos controllers list/card/members. 98 testes módulo board. Frontend: painel de atividade (histórico paginado + ao vivo, textos legíveis). Gate/CI verde. Capability atividade. |
| 012-hardening-http | 2026-07-05 | #21 | Hardening: helmet, limite body 1mb, trust proxy; CORS explícito por CORS_ORIGIN (HTTP+Socket.IO unificados, fail-fast só em produção); rate limit global + estrito em /auth/login e /auth/register → 429 i18n. 23 testes. Gate/CI verde. Capability protecao-http. |
| 013-fundacao-e2e | 2026-07-05 | #22 | E2E: Playwright na raiz (config, helpers registro/login/quadro/membro); smoke de auth; spec VITRINE colaboração ao vivo (2 contextos owner+membro, A move cartão, B vê ao vivo sem reload, 3x sem flake); data-testid na UI; test:e2e (e2e fora do CI padrão). Fix: eco do socket duplicava item na criação. Gate/CI verde. Capability fundacao-e2e. |
| 014-telas-publicas | 2026-07-06 | #25 | v2/telas: Landing pública em / (logado→/boards), /join reestilizado ao mockup (integração real), estados de sistema (404/error/skeletons/banner de reconexão do socket), onboarding de primeiro acesso com ações reais. Mockups do Claude Design dentro da change. Gate/CI verde. Capability telas-publicas. |
