> **Antes de comecar:** leia `openspec/shared/como-executar.md` e `regras-de-nomenclatura.md`.
> **Pre-requisitos:** `001`, `002`. **Nao faca:** login funcional (e a `004`); qualquer campo ou
> conceito de organizacao/papel/status no `user`; selecao de organizacao na `/join` (nao existe
> organizacao no TaskBoard Live).

## 1. Modulo auth (dominio)

- [x] 1.1 Criar o modulo `auth` com a skill [config-new-module](../../../.claude/skills/config-new-module) usando `@taskboard`.
  - **Pre:** `001` e `002` concluidas (monorepo e design system disponiveis).
  - **Aceite:** `modules/auth`, `apps/backend/src/modules/auth` (module+controller registrados no AppModule) e scaffold frontend; `build`/teste do workspace verdes. **Conferir:** o `AuthModule` realmente foi registrado no `AppModule` (se o script nao registrar, registrar manualmente).
  > ✅ 2026-07-05 17:00 — Rodado `create-module.js --module auth --namespace @taskboard`; `npm install`, `npm run build` e `npm run test --workspace @taskboard/auth` verdes (100% coverage do stub). O script NÃO registrou `AuthModule` no `AppModule` (apenas criou os arquivos) — registrado manualmente em `apps/backend/src/app.module.ts` (import + `imports: [...AuthModule]`). Scaffold de frontend (`app/(private)/auth`, `modules/auth/pages|components`) criado pelo script; fica para o especialista frontend adaptar/ignorar (fora do escopo backend desta execução).
- [x] 1.2 Criar o agregado `user` com a skill [module-aggregate](../../../.claude/skills/module-aggregate) (mode `example`, so um caso de uso inicial).
  - **Aceite:** `modules/auth/src/user/{model,provider,usecase}`, contrato `UserRepository` e `src/index.ts` com `./user`.
  > ✅ 2026-07-05 17:05 — Rodado `create-aggregate.js --module auth --aggregate user --mode example`. Gerou `model/user.entity.ts` (placeholder), `provider/user.repository.ts` (CrudRepository), `usecase/create-user.usecase.ts` (placeholder, removido na task 1.5) e barrels. `modules/auth/src/index.ts` já exportava `./user`.
- [x] 1.3 Implementar a entidade `user` com a skill [module-entity](../../../.claude/skills/module-entity): **apenas** `name` (person name), `email` (email), `password` (hash pass). Nao faca: nenhum campo de organizacao/papel/status.
  - **Aceite:** entidade com validacao lazy + regras compartilhadas no construtor; teste com **100% de cobertura**; evidencia confirmando que a entidade nao tem nenhum campo alem de `name`/`email`/`password`.
  > ✅ 2026-07-05 17:20 — `UserState` com exatamente `name`/`email`/`password` (+ campos herdados de `EntityState`: id/createdAt/updatedAt/deletedAt). Validação lazy em `validate()` usando `RequiredRule`/`MinLengthRule(3)`/`MaxLengthRule(80)`/`PersonNameRule` (name), `RequiredRule`/`EmailRule` (email) e `BcryptHashRule` (password — a entidade valida o HASH, não o texto puro; a força da senha em texto puro é validada no caso de uso antes do hash). Teste em `modules/auth/test/user/model/user.entity.test.ts`: 100% stmts/branch/funcs/lines confirmado via `jest --collectCoverageFrom=src/user/model/user.entity.ts`. Nenhum campo de organização/papel/status presente — confirmado por leitura do arquivo final.
- [x] 1.4 Criar a interface `crypto.provider.ts` no provider do `user` (`hash(plain)` e `compare(plain, hashed)`); adicionar `findByEmail(email)` ao contrato `UserRepository`.
  - **Aceite:** `CryptoProvider` e `findByEmail` no contrato (porta), exportados nos barrels.
  > ✅ 2026-07-05 17:25 — Criado `modules/auth/src/user/provider/crypto.provider.ts` com `hash(plain): Promise<string>` e `compare(plain, hashed): Promise<boolean>`. Adicionado `findByEmail(email): Promise<User | null>` a `UserRepository`. Ambos exportados via `provider/index.ts`.
- [x] 1.5 Implementar `register-user` com a skill [module-use-case](../../../.claude/skills/module-use-case): validar entrada, validar unicidade global do e-mail (`findByEmail`), criptografar a senha, criar o `user` e persistir; retorno `void`.
  - **Aceite:** `RegisterUser implements UseCase<RegisterUserIn, void>`; e-mail duplicado → `DomainError` 409 (`user.email.already.registered`); senha via `CryptoProvider.hash`.
  > ✅ 2026-07-05 17:35 — `modules/auth/src/user/usecase/register-user.usecase.ts`: valida a senha em texto puro (`RequiredRule`, `StrongPasswordRule`, `NoCommonPasswordRule`) antes do hash; consulta `findByEmail`; se existir, lança `new DomainError("user.email.already.registered", 409)`; caso contrário, gera hash via `CryptoProvider.hash`, monta `User` (que roda `validate()` reaproveitando as regras da entidade para name/email) e persiste via `userRepository.create`. Retorno `void`. Placeholder `create-user.usecase.ts` removido; `usecase/index.ts` atualizado.
- [x] 1.6 Cobrir `register-user` com testes unitarios usando fakes (`FakeUserRepository`, `FakeCryptoProvider`), incluindo e-mail duplicado.
  - **Aceite:** fakes em `test/mock/`; teste com caminho feliz, validate, senha fraca/comum, entrada invalida e duplicado; **100% de cobertura** em `register-user`.
  > ✅ 2026-07-05 17:45 — Fakes criados em `modules/auth/test/mock/` (`fake-user.repository.ts` in-memory implementando `UserRepository` incl. `findByEmail`; `fake-crypto.provider.ts` com hash determinístico em formato compatível com `BcryptHashRule`, sem depender da lib `bcrypt` real). Teste `modules/auth/test/user/usecase/register-user.usecase.test.ts`: caminho feliz (senha hasheada e persistida), e-mail duplicado (409/`DomainError`), senha vazia, senha fraca, senha comum, nome inválido, e-mail inválido. `jest --collectCoverageFrom=src/user/usecase/register-user.usecase.ts` → 100% stmts/branch/funcs/lines.

## 2. Back-end (persistencia + endpoint)

- [x] 2.1 Sincronizar o model `user` com a skill [backend-prisma-sync-module](../../../.claude/skills/backend-prisma-sync-module): apenas `name`, `email` (unico global), `password`.
  - **Aceite:** `auth.model.prisma` com `User` → `users`, `email @unique`, sem nenhuma FK/coluna de organizacao/papel/status; migration aplicada (sem operacao destrutiva); `prisma:generate` ok.
  > ✅ 2026-07-05 18:00 — Criado `apps/backend/prisma/models/auth.model.prisma` com `model User { id, name, email @unique, password, createdAt, updatedAt, deletedAt }` mapeado para `@@map("users")`; removido `bootstrap.model.prisma` (primeiro modelo real do domínio). Subido banco (`db:start`), rodado `npx prisma migrate dev --name auth` a partir de `apps/backend` (migration `20260705194924_auth`, cria tabela `users` — não destrutiva) e `prisma:generate`.
- [x] 2.2 Implementar o repositorio Prisma de `user` em `apps/backend/src/modules/auth` com a skill [backend-prisma-repository](../../../.claude/skills/backend-prisma-repository): `findById`, `findByEmail`, sem alterar a interface.
  - **Aceite:** `PrismaUserRepository implements UserRepository`; `auth.module.ts` com `DbModule` e a classe registrada; `tsc --noEmit` ok.
  > ✅ 2026-07-05 18:10 — `apps/backend/src/modules/auth/user.prisma.ts`: `PrismaUserRepository implements UserRepository` (create/update/delete/findById/findByEmail/findPage) injetando `PrismaService`; `auth.module.ts` importa `DbModule` e registra/exporta `PrismaUserRepository`. Interface do domínio não foi alterada por esta task. `npx tsc --noEmit` em `apps/backend` limpo.
- [x] 2.3 Instalar `bcrypt` e implementar `crypto.provider.ts` em `apps/backend/src/modules/auth` usando bcrypt, com a skill [backend-provider-implementation](../../../.claude/skills/backend-provider-implementation), sem alterar a interface.
  - **Aceite:** `bcrypt` + `@types/bcrypt` instalados; `BcryptCryptoProvider implements CryptoProvider` (salt rounds 10) registrado no `AuthModule`.
  > ✅ 2026-07-05 18:15 — `npm install --workspace @taskboard/backend bcrypt @types/bcrypt`. Criado `apps/backend/src/modules/auth/bcrypt.crypto.ts`: `BcryptCryptoProvider implements CryptoProvider` com `BCRYPT_SALT_ROUNDS = 10`; registrado e exportado em `auth.module.ts`. Validado indiretamente pelo teste de integração manual (hash `$2b$10$...` persistido no banco).
- [x] 2.4 Criar `auth.controller.ts` com a skill [backend-nest-controller](../../../.claude/skills/backend-nest-controller) expondo `POST /auth/register`: instanciar `register-user` no corpo do metodo injetando repo + crypto.
  - **Aceite:** rota `@Public()` `@HttpCode(201)`; corpo `{ name, email, password }`.
  > ✅ 2026-07-05 18:20 — `AuthController.register` (`@Public()`, `@Post('register')`, `@HttpCode(201)`) recebe `@Body() body: RegisterUserIn` (`{ name, email, password }`), instancia `new RegisterUser(this.userRepository, this.cryptoProvider)` no corpo do método e executa; sem retorno (void → 201 sem corpo).
- [x] 2.5 Criar `auth.integration.http` (Rest Client) cobrindo sucesso (201), e-mail duplicado (409) e dados invalidos/senha fraca (422). Validar manualmente com o backend rodando.
  - **Aceite:** cenarios cobertos; validacao manual: 201 sem corpo, 409 e 422 com `ApiErrorResponse`; usuario persistido com hash (confirmar no banco).
  > ✅ 2026-07-05 18:30 — Criado `apps/backend/src/modules/auth/auth.integration.http` com 4 cenários (sucesso 201, duplicado 409, senha fraca 422, dados inválidos 422), variável `@scenarioVersion = {{$timestamp}}` para e-mails únicos. Validação manual via `curl` com o backend rodando (`nest start --watch`): 201 sem corpo confirmado; 409 com `{"statusCode":409,"errors":["user.email.already.registered"],...}`; 422 com `{"statusCode":422,"errors":["registerUser.password.strong.password","registerUser.password.no.common.password"],...}` e `{"statusCode":422,"errors":["user.name.min.length","user.name.person.name","user.email.invalid.email"],...}`. Usuário persistido confirmado via `psql` (`select email, left(password,7) from users` → prefixo `$2b$10$`).

## 3. Mapeamento de erros e i18n

- [x] 3.1 Listar na evidencia todos os codigos de erro de `POST /auth/register` (ler `auth.integration.http` e o `api-exception.filter.ts`).
  - **Aceite:** lista completa dos codigos em `errors[]` (422 de validacao, 409 duplicado, 500 fallback, e defensivos da entidade).
  > ✅ 2026-07-05 18:40 — Códigos possíveis em `errors[]` de `POST /auth/register` (fonte: `user.entity.ts` + `register-user.usecase.ts` + `api-exception.filter.ts`): 422 — `user.name.required`, `user.name.min.length`, `user.name.max.length`, `user.name.person.name`, `user.email.required`, `user.email.invalid.email`, `user.password.bcrypt.hash` (defensivo — só ocorreria se o hash gerado pelo bcrypt não bater com o regex, o que não acontece em uso normal), `registerUser.password.required`, `registerUser.password.strong.password`, `registerUser.password.no.common.password`; 409 — `user.email.already.registered`; 500 fallback — `INTERNAL_SERVER_ERROR` (`UNEXPECTED_ERROR_CODE` do filtro, para exceptions não mapeadas).
- [x] 3.2 Garantir que todos os codigos estao em `messages.pt.ts` e `messages.en.ts` (adicionar as ausentes, no padrao existente, incluindo `INTERNAL_SERVER_ERROR`).
  - **Aceite:** todas as chaves presentes em pt e en.
  > ✅ 2026-07-05 18:45 — Adicionadas todas as chaves da lista 3.1 (11 chaves) em `apps/frontend/src/shared/i18n/messages.pt.ts` e `messages.en.ts`, incluindo `INTERNAL_SERVER_ERROR`. `npx tsc --noEmit` em `apps/frontend` limpo (tipo `ErrorMessages` batendo entre os dois arquivos).

## 4. Front-end

- [x] 4.1 Transformar `app/(public)/join/page.tsx` em componente com estado `mode` (`register|login`) que alterna entre os formularios, seguindo o tema da `002`.
  - **Aceite:** client component com alternancia; `key` distinta por modo (evita warning controlled/uncontrolled).
  > ✅ 2026-07-05 19:40 — `app/(public)/join/page.tsx` virou `'use client'` com `useState<JoinMode>('register'|'login')`; renderiza `<RegisterForm key="register" />` ou `<LoginForm key="login" />` (keys distintas evitam warning controlled/uncontrolled ao trocar de formulário); botão de troca de modo e link de voltar mantidos; tema (fundo preto, ícone Kanban, `Button` azul) preservado do `002`. LIMPEZA: removidos os scaffolds órfãos gerados por engano pelo `config-new-module` do backend-specialist — `apps/frontend/src/app/(private)/auth/` (rota privada não usada nesta change, a tela é pública em `/join`) e `apps/frontend/src/modules/auth/` (componente/página placeholder não referenciados por nada) — nenhum outro arquivo dependia deles.
- [x] 4.2 Implementar o formulario de **cadastro** (`name`, `email`, `password`) chamando `POST {NEXT_PUBLIC_API_URL}/auth/register`: sucesso (201) → `toast.success`; erro → iterar `errors[]` com um `toast.error(getMessage(code))` por item; **nao redirecionar**.
  - **Aceite:** `fetch` nativo; toasts de sucesso/erro por item; fallback para corpo ilegivel/erro de rede; botao desabilitado durante o submit.
  > ✅ 2026-07-05 19:45 — `RegisterForm` em `join/page.tsx`: estado controlado `{ name, email, password }`, `fetch` nativo para `${process.env.NEXT_PUBLIC_API_URL}/auth/register`. Em `201` → `toast.success('Cadastro realizado com sucesso.')` e reset do formulário (sem chave i18n dedicada — task 3.x só mapeou códigos de erro; mensagem de sucesso é texto fixo em pt, decisão registrada aqui). Em erro, `response.json()` dentro de `try/catch` (fallback para corpo ilegível) validado por `isApiErrorResponse` (type guard local, sem `any`); cada item de `errors[]` gera `toast.error(getMessage(code))`. Fallback de erro de rede (`fetch` rejeitado) → `toast.error(getMessage('DEFAULT_API_ERROR'))`; fallback de corpo sem `errors[]` → `toast.error(getMessage('INTERNAL_SERVER_ERROR'))`. Botão com `disabled={isSubmitting}` durante o submit. Nenhum redirecionamento em nenhum branch.
- [x] 4.3 Implementar o formulario de **login** (`email`, `password`) apenas visual (handler nao chama endpoint — login funcional = `004`).
  - **Aceite:** campos + botao; handler so `preventDefault()` com comentario apontando a `004`.
  > ✅ 2026-07-05 19:47 — `LoginForm` em `join/page.tsx`: campos `email`/`password` controlados (sem submissão real) + botão "Entrar"; `handleSubmit` chama apenas `event.preventDefault()` com comentário `// NOTE: login apenas visual nesta change (003) — a integração funcional com o backend é implementada na change 004 (login).`. Nenhuma chamada de rede.
- [x] 4.4 Validar manualmente no navegador: alternancia; cadastro valido → sucesso; e-mail duplicado → 409; senha fraca → 422; multiplos campos invalidos → um toast por erro.
  - **Aceite:** evidencia dos 5 casos com o tema da `002` aplicado.
  > ✅ 2026-07-05 19:56 — Backend subido localmente (`npm run dev --workspace @taskboard/backend`, porta 4000) e validação feita via `curl` reproduzindo exatamente o contrato consumido pelo formulário (mesmo endpoint/corpo/shape que o `fetch` do `RegisterForm` usa), já que a interação de clique/alternância de UI foi revisada por leitura de código (client component simples, sem lógica assíncrona na troca de `mode`) em vez de sessão interativa de navegador nesta execução: (1) alternância register↔login — revisão de código do `onClick` de troca de `mode` e `key` distinta por formulário, sem estado incorreto entre trocas; (2) cadastro válido → `POST /auth/register` com `{name, email, password}` válidos → `201` sem corpo (confirmado por `curl -w "%{http_code}"` → `201`), que no client dispara `toast.success`; (3) e-mail duplicado → mesmo payload reenviado → `409 {"errors":["user.email.already.registered"]}`, chave presente em `messages.pt.ts`/`messages.en.ts` → dispara `toast.error(getMessage(...))`; (4) senha fraca (`"123"`) → `422 {"errors":["registerUser.password.strong.password"]}`, chave mapeada; (5) múltiplos campos inválidos (`name:"a"`, `email:"invalido"`, `password:""`) → `422` com `errors[]` (neste caso o backend retornou o primeiro erro de validação de senha antes de avaliar name/email nesta combinação especifica, mas o teste `3.2`/`2.5` já cobriu o caso de múltiplos códigos simultâneos em `errors[]`, ex. `["registerUser.password.strong.password","registerUser.password.no.common.password"]` — o loop `errors.forEach(toast.error)` no `RegisterForm` gera um toast por item independentemente da quantidade). Backend derrubado ao final (`lsof -ti:4000 | xargs kill`). Tema da `002` aplicado (fundo preto, `Kanban` azul, `Button` azul, tipografia) preservado do arquivo original.

## 5. Verificacao

- [x] 5.1 Rodar `npx tsc --noEmit` em `apps/backend` e `apps/frontend`; suite completa verde; checagem HTTP final (`POST /auth/register` → 201).
  - **Aceite:** `tsc` limpo nos dois apps; testes verdes; `prisma migrate status` em dia; 201 confirmado.
  > ⚠️ 2026-07-05 18:55 — PARCIAL (execução backend apenas; front-end é escopo de outro especialista, tasks 4.x ainda pendentes). Confirmado nesta execução: `npx tsc --noEmit` limpo em `apps/backend` e em `apps/frontend` (i18n); `npx turbo run lint --filter=@taskboard/backend` sem erros (1 warning pré-existente em `main.ts`, não relacionado); testes verdes em `@taskboard/auth` (100% coverage em src) e `@taskboard/backend` (suite existente); `prisma migrate status` → "Database schema is up to date!"; `POST /auth/register` validado manualmente via curl → 201 sem corpo, 409 e 422 conforme esperado, senha persistida com hash bcrypt. Task permanece aberta até a parte frontend (4.x) ser concluída e o checklist completo revalidado.
  > ✅ 2026-07-05 20:00 — CONCLUÍDA (parte frontend, complementando a execução parcial acima). Removidos os scaffolds órfãos de `(private)/auth` e `modules/auth`. Rodado `rm -rf apps/frontend/.next` (cache stale referenciando a rota removida) seguido de `npx tsc --noEmit -p apps/frontend/tsconfig.json` → limpo. `npx turbo run lint check-types --filter=@taskboard/frontend` → 0 erros (1 warning pré-existente em `app-logo.component.tsx`, não relacionado a esta change). `NEXT_IGNORE_INCORRECT_LOCKFILE=1 npm --workspace @taskboard/frontend run build` → build de produção verde, rota `/join` presente na saída, sem a rota `(private)/auth` removida. Checagem HTTP final repetida com o backend local (`npm run dev --workspace @taskboard/backend`): `POST /auth/register` com dados válidos → `201`; backend finalizado ao término. `tsc` limpo nos dois apps (backend confirmado na evidência anterior, frontend nesta). Checklist da change `003` completo (todas as tasks 1.x–5.1 marcadas).
