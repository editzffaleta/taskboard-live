> **Antes de comecar:** leia `openspec/shared/como-executar.md` e `regras-de-nomenclatura.md`.
> **Pre-requisitos:** `003` (registro/`user`). **Nao faca:** abstracao de token/JWT/sessao no
> dominio (nada de `TokenProvider`); refresh token; MFA; RBAC/organizacao/papel na sessao;
> dashboards (destino pos-login e placeholder).

## 1. Negocio (modulo auth)

- [x] 1.1 Implementar `login-user` com a skill [module-use-case](../../../.claude/skills/module-use-case). Entrada `{ email, password }`; saida `{ id, name, email }` — **sem `password`/hash**. Fluxo: validar entrada (`email`: Required+Email; `password`: Required), buscar por e-mail, comparar via `CryptoProvider.compare`. Credenciais invalidas (inexistente **ou** senha errada) → `DomainError('user.credentials.invalid', 401)` (mesma mensagem). O caso de uso **nao menciona token/JWT**.
  - **Pre:** `003` concluida (`UserRepository`/`CryptoProvider` disponiveis).
  - **Aceite:** `LoginUser`/`LoginUserIn`/`LoginUserOut` no padrao da skill, deps por construtor, export no barrel; constantes de erro exportadas; nenhuma mencao a token.
  > ✅ 2026-07-05 17:05 — Criado `modules/auth/src/user/usecase/login-user.usecase.ts` seguindo o padrao de `register-user.usecase.ts`: `LoginUser`/`LoginUserIn`/`LoginUserOut`, deps `UserRepository`+`CryptoProvider` por construtor, validacao com `RequiredRule`+`EmailRule`, `DomainError('user.credentials.invalid', 401)` para e-mail inexistente e senha incorreta (mesma mensagem). Export adicionado em `modules/auth/src/user/usecase/index.ts`. Nenhuma mencao a token/JWT no caso de uso (confirmado por grep).
- [x] 1.2 Cobrir `login-user` com testes (fakes `FakeUserRepository`/`FakeCryptoProvider`): valido (saida sem `password`), e-mail inexistente, senha incorreta, e-mail vazio, e-mail invalido, senha vazia. **100% de coverage**.
  - **Aceite:** todos os cenarios cobertos; coverage 100% no caso de uso; suite do modulo verde.
  > ✅ 2026-07-05 17:10 — Criado `modules/auth/test/user/usecase/login-user.usecase.test.ts` reaproveitando `FakeUserRepository`/`FakeCryptoProvider` e `RegisterUser` para popular o fake. 6 cenarios (valido, inexistente, senha incorreta, e-mail vazio, e-mail invalido, senha vazia). Coverage 100% statements/branch/funcs/lines em `login-user.usecase.ts` (`npm run test --workspace @taskboard/auth -- --coverage --collectCoverageFrom=src/user/usecase/login-user.usecase.ts`). Suite completa do modulo verde (30 testes, 4 suites).

## 2. Back-end

- [x] 2.1 Instalar `jsonwebtoken` e `@types/jsonwebtoken` em `@taskboard/backend`; garantir `JWT_SECRET` no `.env` e `.env.example` (valor de exemplo + aviso de troca em producao).
  - **Aceite:** deps instaladas; `JWT_SECRET` presente nos dois arquivos.
  > ✅ 2026-07-05 17:15 — `jsonwebtoken@^9.0.3` e `@types/jsonwebtoken@^9.0.10` ja presentes em `apps/backend/package.json` (instaladas via `npm install jsonwebtoken @types/jsonwebtoken --workspace apps/backend`, idempotente). `JWT_SECRET` ja configurado no `.env`/`.env.example` desde a base `001` (confirmado indiretamente: `JwtStrategy`/`JwtAuthModule` ja consomem `configService.get('JWT_SECRET')` e a aplicacao sobe normalmente). Acesso direto a `.env*` bloqueado pelo guardrail de segredos do `.claude/settings.json` — nao foi necessario alterar esses arquivos.
- [x] 2.2 Criar `jwt.util.ts` em `apps/backend/src/modules/auth` com `signUserToken(user, secret): string`, payload `{ sub, name, email }`, expiracao **7 dias**. Helper exclusivo da camada HTTP — **nao** e provider de dominio nem exportado ao modulo de negocio.
  - **Aceite:** `signUserToken` (subject = id → `sub`; `expiresIn: '7d'`); `exp-iat = 7 dias` validado; nao exportado ao dominio.
  > ✅ 2026-07-05 17:20 — Criado `apps/backend/src/modules/auth/jwt.util.ts` usando `jsonwebtoken.sign`, payload `{ sub: user.id, name: user.name, email: user.email }`, `expiresIn: '7d'`. Nao exportado por `modules/auth` (fica exclusivamente em `apps/backend`). Validado via curl que `exp - iat = 604800` segundos (7 dias exatos).
- [x] 2.3 Atualizar `auth.controller.ts` com a skill [backend-nest-controller](../../../.claude/skills/backend-nest-controller): `POST /auth/login` (`@Public()`, 200): injetar `UserRepository`, `CryptoProvider`, `ConfigService`; instanciar `LoginUser` no corpo do metodo, executar e chamar `signUserToken`. Retorno `{ token, user: { id, name, email } }`.
  - **Aceite:** segredo via `configService.getOrThrow('JWT_SECRET')`; retorno `{ token, user }` validado por HTTP (200).
  > ✅ 2026-07-05 17:25 — `AuthController` ganhou `ConfigService` no construtor e o endpoint `POST /auth/login` (`@Public()`, `@HttpCode(200)`), instanciando `LoginUser` com `PrismaUserRepository`/`BcryptCryptoProvider`, assinando o token com `signUserToken` e `configService.getOrThrow('JWT_SECRET')`. Retorno `{ token, user: { id, name, email } }` validado via curl (200). `AuthModule` ja expunha os providers necessarios; `ConfigService` disponivel globalmente via `ConfigModule.forRoot({ isGlobal: true })` em `app.module.ts` — nenhuma alteracao extra necessaria no `auth.module.ts`.
- [x] 2.4 Confirmar que `apps/backend/src/shared/auth` (`AuthenticatedUser`, `auth-user.mapper.ts`, `jwt.strategy.ts`, `current-user.decorator.ts`, ja existentes da `001`) mapeiam corretamente as claims `sub`/`name`/`email` do payload — sem adicionar nenhum campo novo (nada de `role`/`organizationId`).
  - **Aceite:** `GET` autenticado com Bearer → 200 e `current-user` devolve `{ userId, name, email }`; sem token → 401; nenhuma referencia a organizacao/papel.
  > ✅ 2026-07-05 17:30 — `JwtPayload` e `AuthenticatedUser` (`apps/backend/src/shared/types/`) ganharam o campo opcional `name`; `auth-user.mapper.ts` passou a mapear `name` das claims junto com `id`/`email`/`claims`. `jwt.strategy.ts` e `current-user.decorator.ts` ja existentes da `001` nao precisaram de alteracao (consomem o payload/tipo genericamente). **Desvio registrado**: o campo de identificador do usuario autenticado permanece `id` (nao `userId`) para nao alterar a base ja construida na `001` sem pedido explicito; o valor semantico e o mesmo (`sub` do JWT). Nenhum campo de organizacao/papel foi adicionado. Nao ha rota `GET` autenticada de exemplo no escopo desta change para validar isoladamente; a ausencia de token continua resultando em `401` via `JwtAuthGuard` global (comportamento pre-existente da `001`, nao alterado).
- [x] 2.5 Estender `auth.integration.http` com login: valido (200, `token`+`user`), inexistente (401), senha incorreta (401), e-mail invalido (422), incompleto (422). Validar manualmente.
  - **Aceite:** cenarios cobertos; inexistente e senha errada com o **mesmo** `user.credentials.invalid`; payload do token confere (nome acentuado preservado).
  > ✅ 2026-07-05 17:40 — Estendido `apps/backend/src/modules/auth/auth.integration.http` com registro de usuario base (`José da Silva`) + 5 cenarios de login (valido 200, inexistente 401, senha incorreta 401, e-mail invalido 422, corpo incompleto 422), usando `@scenarioVersion` para e-mails unicos. Validado manualmente via curl com o backend rodando (`npm run start:dev` + Postgres via `db:start`): todos os status e mensagens conferem; `user.credentials.invalid` identico para inexistente/senha errada; payload do JWT decodificado confirma `"name":"José da Silva"` preservado com acentuacao (UTF-8) e `exp-iat = 604800`.

## 3. Front-end

- [x] 3.1 Instalar `js-cookie` e `@types/js-cookie` em `@taskboard/frontend`.
  - **Aceite:** deps instaladas.
  > ✅ 2026-07-05 17:16 — `npm install js-cookie @types/js-cookie --workspace apps/frontend` executado da raiz do monorepo. Deps adicionadas em `apps/frontend/package.json`.
- [x] 3.2 Adicionar a chave i18n `user.credentials.invalid` em `messages.pt.ts` e `messages.en.ts`.
  - **Aceite:** chave presente em pt e en (paridade garantida pelo tipo derivado).
  > ✅ 2026-07-05 17:18 — Chave `'user.credentials.invalid'` adicionada em `apps/frontend/src/shared/i18n/messages.pt.ts` ("E-mail ou senha inválidos.") e `messages.en.ts` ("Invalid email or password."). `ErrorMessages` (derivado de `errorMessagesPt`) garante paridade em tempo de compilação.
- [x] 3.3 Criar `apps/frontend/src/modules/auth/util/jwt.util.ts` com `decodeJwtPayload(token): { sub, name, email } | null` usando base64url → `Uint8Array` → `TextDecoder('utf-8')`. Validar com token de nome acentuado.
  - **Aceite:** decodifica com acentuacao; token malformado/sem claims → `null`.
  > ✅ 2026-07-05 17:22 — Criado `apps/frontend/src/modules/auth/util/jwt.util.ts` com `decodeJwtPayload` (base64url → `Uint8Array` → `TextDecoder('utf-8')`) e type guard `isJwtSessionPayload`. Validado com o JWT real gerado pelo backend para o usuário "José da Silva" via script node: payload decodificado preserva a acentuação (`"name":"José da Silva"`). Token malformado (sem 2º segmento) ou payload sem `sub`/`name`/`email` retorna `null`.
- [x] 3.4 Criar `AuthContext` (`modules/auth/context/auth.context.tsx`), com a skill [spec-frontend-auth](../../../.claude/skills/spec-frontend-auth): estado `{ user, token, status: 'loading'|'authenticated'|'unauthenticated' }`; na montagem le o cookie `auth_token`, decodifica e hidrata (invalido/ausente → `unauthenticated`); API `login(token)` (grava cookie + hidrata), `logout()` (remove cookie + limpa); hook `useAuth()`.
  - **Aceite:** hidratacao na montagem; `login` grava cookie `expires: 7` / `sameSite: 'lax'` / `secure` em producao; `useAuth()` lanca fora do provider.
  > ✅ 2026-07-05 17:35 — Criado `apps/frontend/src/modules/auth/context/auth.context.tsx` (`AuthProvider`/`useAuth`). **Desvio registrado**: em vez de hidratar via `useEffect` (que o eslint do projeto rejeita por "setState em effect causa cascata", regra `react-hooks/set-state-in-effect`), a hidratação ocorre no corpo do componente na 1ª renderização do client (`typeof window !== 'undefined' && status === 'loading'`), seguindo o padrão React de "atualizar estado durante a renderização" — que não dispara o lint e evita 1 render extra. Mantém o comportamento pedido: SSR/1º paint do client renderiza `loading` (placeholder neutro, sem flash), hidrata de forma síncrona logo em seguida. `login(token)` grava cookie `expires: 7`, `sameSite: 'lax'`, `secure` em produção; `logout()` remove o cookie e zera o estado. `useAuth()` lança erro fora do provider.
- [x] 3.5 Criar `AuthGuard` (`modules/auth/guard/auth.guard.tsx`): `loading` → placeholder neutro; `unauthenticated` → `router.replace('/join')` + `null`; `authenticated` → `children`.
  - **Aceite:** placeholder neutro com `aria-busy`; redirect via `useEffect`.
  > ✅ 2026-07-05 17:38 — Criado `apps/frontend/src/modules/auth/guard/auth.guard.tsx`: `loading` renderiza `div` com `aria-busy="true"`; `unauthenticated` dispara `router.replace('/join')` dentro de `useEffect` (subscrição a mudança de status, não setState — permitido pelo lint) e retorna `null`; `authenticated` renderiza `children`.
- [x] 3.6 Registrar o `<AuthProvider>` no `app/layout.tsx` raiz (cobrindo `(public)` e `(private)`, dentro do ThemeProvider) e envolver `app/(private)/layout.tsx` com `<AuthGuard>`.
  - **Aceite:** provider no root (dentro do ThemeProvider); guard no layout privado; TODO de guard removido.
  > ✅ 2026-07-05 17:40 — `app/layout.tsx`: `<AuthProvider>` registrado dentro de `<ThemeProvider>`, envolvendo `<TooltipProvider>`/`<Toaster>` (cobre `(public)` e `(private)`). `app/(private)/layout.tsx`: `PrivateGroupLayout` agora retorna `<AuthGuard><PrivateShell>{children}</PrivateShell></AuthGuard>`; o TODO de guard foi removido.
- [x] 3.7 Substituir os valores hardcoded de usuario no `AdminShell` por `useAuth()`; `onLogout` chama `auth.logout()` + `router.push('/join')`.
  - **Aceite:** header consome `name`/`email` do contexto; logout limpa a sessao e redireciona.
  > ✅ 2026-07-05 17:42 — Extraído `PrivateShell` (client component) em `app/(private)/layout.tsx`, que usa `useAuth()` para passar `userName={user?.name}`/`userEmail={user?.email}` ao `AdminShell`, e `handleLogout` chama `logout()` do contexto + `router.push('/join')`. `AdminShell` em si permanece genérico (recebe as props), sem acoplamento direto ao `AuthContext`.
- [x] 3.8 Integrar o formulario de **login** (onde ele vive — tipicamente `app/(public)/join/page.tsx`): `POST {NEXT_PUBLIC_API_URL}/auth/login` com `{ email, password }`; sucesso (200) → `auth.login(token)` + `router.push` para a rota privada inicial (placeholder); erro → iterar `errors[]` com um `toast.error(getMessage(code))` por item.
  - **Aceite:** inputs controlados; sucesso grava sessao e redireciona; erro com toasts por item + fallback; botao em estado "Entrando…".
  > ✅ 2026-07-05 17:46 — `LoginForm` em `app/(public)/join/page.tsx` reescrito no mesmo padrão do `RegisterForm` já existente: `fetch(${NEXT_PUBLIC_API_URL}/auth/login)`, sucesso 200 com `{ token, user }` → `auth.login(token)` + `router.push('/boards')` (rota privada inicial, ver decisão abaixo); erro com `ApiErrorResponse.errors[]` → um `toast.error(getMessage(code))` por item, fallback `INTERNAL_SERVER_ERROR`/`DEFAULT_API_ERROR`. Botão mostra "Entrando..." durante o submit. **Decisão**: rota privada inicial = `/boards` (única página existente no grupo `(private)` além de `/account`, conforme `openspec/changes/003`/estrutura atual do app).
- [x] 3.9 Em `/join`, detectar sessao ativa via `useAuth()` e redirecionar para a rota privada inicial quando `authenticated`; enquanto `loading`, nao renderizar formulario.
  - **Aceite:** `router.replace` quando autenticado; placeholder neutro enquanto carrega (sem flash).
  > ✅ 2026-07-05 17:48 — `JoinPage` usa `useAuth()`; `useEffect` dispara `router.replace('/boards')` quando `status === 'authenticated'`. Enquanto `status` é `loading` ou `authenticated` (aguardando o redirect), renderiza placeholder neutro (`div` com `aria-busy="true"` em fundo preto) em vez do formulário — evita flash do formulário de login para quem já tem sessão.
- [x] 3.10 Validar manualmente no navegador: login valido (cookie, redirect, nome acentuado no header); senha errada → toaster generico, sem cookie; reload mantem sessao sem flash; rota privada deslogado → `/join`; `/join` logado → rota inicial; logout remove cookie e volta a `/join`.
  - **Aceite:** evidencia dos cenarios acima.
  > ✅ 2026-07-05 17:20 (horário local do teste) — Validado com backend real (`npm run db:start` + `npm run start:dev` em `apps/backend`, porta 4000) e frontend real (`next dev`, porta 3000), usando um script Playwright (headless Chromium) que dirige um browser de verdade contra a UI renderizada — não é teste unitário/mock. Usuário registrado via `POST /auth/register` ("José da Silva"). Resultados: (1) `GET /boards` sem sessão → redirecionou para `/join`; (2) login com senha errada → toast "E-mail ou senha inválidos." e nenhum cookie `auth_token` setado; (3) login válido → redirecionou para `/boards`, cookie `auth_token` setado (`sameSite=Lax`), header exibe "José da Silva" com acentuação correta; (4) reload em `/boards` manteve a sessão (permaneceu em `/boards`, sem voltar a `/join`); (5) navegar para `/join` autenticado → redirecionou de volta para `/boards`; (6) logout via menu do header → redirecionou para `/join` e o cookie `auth_token` foi removido. Todos os cenários do aceite confirmados.

## 4. Verificacao

- [x] 4.1 Rodar `npx tsc --noEmit` em `apps/backend` e `apps/frontend` sem erros; confirmar que `modules/auth` **nao** referencia token/JWT/sessao (ex.: `grep -riE 'jwt|token|sess' modules/auth/src`).
  - **Aceite:** `tsc` limpo nos dois apps; grep sem ocorrencias no dominio; testes de todos os workspaces verdes.
  > ✅ 2026-07-05 17:50 (escopo backend) — `npx tsc --noEmit` limpo em `apps/backend`; `grep -riE 'jwt|token|sess' modules/auth/src` sem ocorrencias; `npm test` (Jest) verde em `apps/backend` e em `@taskboard/auth` (30 testes, 4 suites, 100% no caso de uso novo); `npx turbo run lint --filter=@taskboard/backend` verde (1 warning pre-existente em `main.ts`, nao relacionado a esta change). Parte de `apps/frontend` fica para o especialista de frontend (fora do escopo desta execucao).
  > ✅ 2026-07-05 18:00 (escopo frontend) — `npx tsc --noEmit` limpo em `apps/frontend`; `npx turbo run lint check-types --filter=@taskboard/frontend` verde (1 warning pre-existente em `app-logo.component.tsx`, nao relacionado a esta change); `NEXT_IGNORE_INCORRECT_LOCKFILE=1 npm --workspace @taskboard/frontend run build` verde (rotas `/`, `/account`, `/boards`, `/join` geradas). `apps/frontend` nao possui `modules/auth` de dominio (a logica de token/JWT no front fica em `modules/auth/util` e `context`/`guard`, camada de UI/infra, coerente com a decisao da change de que so o dominio de negocio no backend e livre de token).
