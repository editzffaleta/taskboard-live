<!-- TEMPLATE — tasks do compartilhamento de membros. Checkboxes vazios; marque com evidencia.
Cada task tem **Aceite** e **Pre**. Placeholders: TaskBoard Live, taskboard. -->

> **Antes de comecar:** leia `openspec/shared/como-executar.md` e `regras-de-nomenclatura.md`.
> **Pre-requisitos:** `005` (Board + BoardMember), `006` (RealtimeEmitter), `009` (pagina do
> quadro). **Nao faca:** convite por e-mail transacional/token; papeis alem de owner/member;
> transferencia de ownership; alterar o schema Prisma de Board/BoardMember. **Principio:**
> BoardMember ja existe desde a 005 — aqui so entram casos de uso, endpoints e UI.

## 1. Back-end

- [x] 1.1 Implementar o caso de uso `add-member` em `apps/backend/src/modules/board/application/use-cases/add-member.use-case.ts` (skill [module-use-case](../../../.claude/skills/module-use-case)): recebe `{ boardId, requesterId, email }`; verifica que `requesterId` e `owner` do quadro (`DomainError('board.owner.required', 403)` caso contrario); resolve `email` para `User` (`DomainError('board.member.not.found', 404)` se nao existir conta); verifica que o `User` resolvido ainda nao e `BoardMember` do quadro (`DomainError('board.member.already.exists', 409)` caso ja seja); cria `BoardMember` `role='member'`.
  - **Pré:** `005` concluída (repositórios de `board`/`membership` e `User` disponíveis).
  - **Aceite:** teste unitário cobre owner adicionando com sucesso, não-owner tentando (403), e-mail sem conta (404), e-mail já membro (409).
  - Não faça: enviar e-mail de convite ou gerar token.
  > ✅ 2026-07-05 21:56 — Implementado em `modules/board/src/membership/usecase/add-member.usecase.ts` (não em `apps/backend/...`): o repositório real da change 010 segue o padrão já estabelecido nas changes 005-009 (`modules/<modulo>/src/<agregado>/usecase/*.usecase.ts`, pacote `@taskboard/board`, consumido pelos controllers do `apps/backend`), confirmado lendo `board.controller.ts`/`card.controller.ts` e a skill `module-use-case`. Desvio de path do `design.md` registrado aqui e mantido consistente com o restante do módulo. Resolução de e-mail via nova porta `MemberDirectory` (`modules/board/src/membership/provider/member-directory.provider.ts`) — evita que `@taskboard/board` passe a depender de `@taskboard/auth` (regra de dependência), satisfeita no `apps/backend` por `MemberDirectoryAdapter` que delega ao `UserRepository`/`PrismaUserRepository` do módulo `auth`. Testes unitários em `modules/board/test/membership/usecase/add-member.usecase.test.ts` (owner com sucesso, não-owner 403, e-mail sem conta 404, e-mail já membro 409, entrada inválida) — 6 testes verdes.
- [x] 1.2 Implementar o caso de uso `remove-member` em `apps/backend/src/modules/board/application/use-cases/remove-member.use-case.ts`: recebe `{ boardId, requesterId, targetUserId }`; verifica `requesterId` owner (`DomainError('board.owner.required', 403)`); verifica que `targetUserId` não é o owner do quadro (`DomainError('board.owner.cannot.be.removed', 403)`); remove o `BoardMember` correspondente.
  - **Pré:** 1.1 concluída (mesma pasta e padrão de erros).
  - **Aceite:** teste unitário cobre owner removendo membro comum com sucesso, não-owner tentando (403), tentativa de remover o próprio owner (403).
  - Não faça: permitir que o próprio owner se remova por este caso de uso.
  > ✅ 2026-07-05 21:56 — Implementado em `modules/board/src/membership/usecase/remove-member.usecase.ts` (mesmo desvio de path documentado na 1.1). Estendido `MembershipRepository` (`modules/board/src/membership/provider/membership.repository.ts`) com `delete(boardId, userId)` e `listByBoardId(boardId)`; implementado em `PrismaMembershipRepository` (`apps/backend/src/modules/board/membership.prisma.ts`) via `prisma.boardMember.delete({ where: { boardId_userId } })`/`findMany`. Testes em `modules/board/test/membership/usecase/remove-member.usecase.test.ts` (owner remove membro comum, não-owner 403, remoção do próprio owner 403, entrada inválida) — 4 testes verdes.
- [x] 1.3 Implementar o caso de uso `list-members` em `apps/backend/src/modules/board/application/use-cases/list-members.use-case.ts`: recebe `{ boardId, requesterId }`; verifica que `requesterId` é `BoardMember` do quadro, qualquer role (`DomainError('board.not.found', 404)` caso contrário); retorna a lista de membros com dados básicos do `User` (`id`, `name`, `email`, `role`).
  - **Pré:** 1.1 concluída.
  - **Aceite:** teste unitário cobre membro comum listando com sucesso e usuário sem membership recebendo 404.
  > ✅ 2026-07-05 21:56 — Implementado em `modules/board/src/membership/usecase/list-members.usecase.ts` (mesmo desvio de path). Usa `membershipRepository.listByBoardId` + `MemberDirectory.findById` para compor `{ userId, name, email, role }`. Testes em `modules/board/test/membership/usecase/list-members.usecase.test.ts` (membro comum lista com sucesso, não-membro recebe `NotFoundError` 404) — 2 testes verdes.
- [x] 1.4 Expor os endpoints sob `/boards/:boardId/members` (skill [backend-nest-controller](../../../.claude/skills/backend-nest-controller)): `GET` (chama `list-members`), `POST` `{ email }` (chama `add-member`), `DELETE /members/:userId` (chama `remove-member`). Reaproveitar `BoardController` da `005` se estiver no mesmo módulo e legível, ou criar `members.controller.ts` — registrar a escolha na evidência.
  - **Pré:** 1.1, 1.2, 1.3 concluídas.
  - **Aceite:** `GET`/`POST`/`DELETE` respondem com os códigos HTTP corretos (200, 201/200, 204) e propagam os erros de domínio (403/404/409) via filtro global de erros da `001`.
  - Não faça: expor os endpoints sem o guard de autenticação JWT global.
  > ✅ 2026-07-05 21:56 — Criado `apps/backend/src/modules/board/members.controller.ts` (arquivo dedicado — `BoardController` já concentra CRUD de quadro, evitou-se inchar o arquivo existente), registrado em `BoardModule` (`apps/backend/src/modules/board/board.module.ts`), que agora importa `AuthModule` para obter o `PrismaUserRepository` via o novo `MemberDirectoryAdapter` (`apps/backend/src/modules/board/member-directory.provider.ts`). Endpoints usam o `JwtAuthGuard` global (`APP_GUARD` em `app.module.ts`) — nenhuma rota marcada `@Public()`. Validado com `curl` real (ver task 3.1): `GET` 200, `POST` 201/404/403, `DELETE` 204/403.
- [x] 1.5 Emitir `member.added` via `RealtimeEmitter` (porta da `006`) logo após `add-member` retornar com sucesso, com payload `{ boardId, user: { id, name, email }, role: 'member' }`.
  - **Pré:** 1.1 e 1.4 concluídas; `006` disponível (`RealtimeEmitter` injetável e exportado do `BoardModule`).
  - **Aceite:** teste de integração confirma que, após `POST /boards/:boardId/members` com sucesso, `emitToBoard` é chamado com o evento e payload corretos; nenhuma emissão ocorre se o caso de uso falhar.
  - Não faça: emitir o evento antes da confirmação de sucesso do caso de uso.
  > ✅ 2026-07-05 21:56 — `MembersController.add` chama `this.realtimeEmitter.emitToBoard(boardId, 'member.added', { boardId, user: { id, name, email }, role })` apenas depois que `AddMember.execute` retorna com sucesso. Teste de integração HTTP em `apps/backend/src/modules/board/members.controller.spec.ts` confirma a chamada com o payload exato no sucesso e ausência de chamada nos casos 404/403. Confirmado também via socket real (script descartável em `verify-member-added.js`, removido após uso): evento `member.added` recebido pelo cliente conectado com o payload `{ boardId, user: { id, name, email }, role: 'member' }`.
- [x] 1.6 Adicionar as chaves i18n de erro (pt/en): `board.member.not.found`, `board.member.already.exists`, `board.owner.required`, `board.owner.cannot.be.removed`.
  - **Pré:** 1.1 e 1.2 concluídas (nomes de erro definidos).
  - **Aceite:** as quatro chaves existem em pt e en e são usadas pelo filtro global de erros da `001`.
  > ✅ 2026-07-05 21:56 — Adicionadas em `apps/frontend/src/shared/i18n/messages.pt.ts` e `messages.en.ts` (`board.owner.required` já existia desde a 005); acrescentadas `board.member.not.found`, `board.member.already.exists`, `board.owner.cannot.be.removed`, além das chaves de validação `addMember.*`/`removeMember.*`/`listMembers.*` seguindo o padrão de `renameBoard.*`.

## 2. Front-end

- [x] 2.1 Criar o painel "Compartilhar" na página do quadro (`009`), em `apps/frontend/app/(private)/boards/[id]/` (skill [frontend-next-config](../../../.claude/skills/frontend-next-config)): lista os membros via `GET /boards/:boardId/members`, exibe nome/e-mail/role de cada um.
  - **Pré:** `009` concluída (página do quadro existe e está navegável).
  - **Aceite:** o painel renderiza a lista de membros ao abrir o quadro, para qualquer membro (owner ou member).
  > ✅ 2026-07-05 22:40 — Criado `apps/frontend/src/modules/boards/components/members-panel.component.tsx` (não em `app/(private)/boards/[id]/`, que no repo real é apenas a rota fina `page.tsx`; toda a UI de quadro vive em `src/modules/boards/components/`, mesmo padrão de `009` — `board-page.component.tsx`/`board-view.component.tsx`). Desvio de path do `design.md` registrado aqui, consistente com o restante do módulo. Novo client de API `apps/frontend/src/modules/boards/api/members.api.ts` (`listMembers`/`addMember`/`removeMember`), reaproveitando o padrão `request`/`BoardsApiError` de `boards.api.ts`. O painel é um `Dialog` (reaproveita `shared/components/ui/dialog.tsx` e `standard-dialog-content.tsx`) acionado por um botão "Compartilhar" na toolbar (`board-toolbar.component.tsx`); ao abrir, faz `GET /members` e repassa o resultado para o estado do `BoardView` (fonte de verdade única, task 2.3). Badge "proprietário"/"membro" via `shared/components/ui/badge.tsx`.
- [x] 2.2 Adicionar o formulário de convite por e-mail (`POST /boards/:boardId/members`) e o botão de remover por membro (`DELETE /boards/:boardId/members/:userId`), visíveis **apenas** quando o usuário atual é `owner` do quadro.
  - **Pré:** 2.1 concluída; dado de role do usuário atual disponível (via `GET /boards/:boardId` da `005` ou equivalente).
  - **Aceite:** owner vê e usa convidar/remover com sucesso; membro comum não vê os controles de gestão; erros `board.member.not.found`/`board.member.already.exists` aparecem como feedback legível no formulário.
  - Não faça: renderizar os controles de gestão para quem não é owner, mesmo que escondidos via CSS.
  > ✅ 2026-07-05 22:40 — `isOwner` calculado em `board-view.component.tsx` como `user?.id === board.ownerId` (`AuthContext` já expõe `user.id`; `BoardState`/`BoardDetail` já expõem `ownerId` desde a `005` — adicionado `ownerId` a `BoardState`/`toBoardState` em `board-page.component.tsx`, que faltava). Formulário de convite e botão de remover (ícone `X`) só são renderizados (não apenas ocultados via CSS) quando `isOwner === true`, condicional em JSX dentro de `members-panel.component.tsx` — verificado lendo o componente renderizado, nenhum controle de gestão fica no DOM para não-owner. Owner não pode remover a si mesmo nem o próprio owner (`member.role !== 'owner'`). Erros de `addMember`/`removeMember` (`board.member.not.found`, `board.member.already.exists`, `board.owner.required`, `board.owner.cannot.be.removed`) tratados via `BoardsApiError`/`getMessage(code)` + `toast.error`, mesmo padrão de `board-view.component.tsx`. Confirmação de remoção via `window.confirm` (mensagem i18n `membersPanel.removeConfirmTitle`/`removeConfirmDescription`) — desvio simplificado do `DeleteConfirmationDialog` de `shared/` (que exige digitar uma palavra), por ser uma ação reversível (re-convidar) e de baixo risco num painel compacto; registrado aqui como decisão.
- [x] 2.3 Conectar o painel ao evento `member.added` do socket já aberto pela `009`: ao receber o evento para o `boardId` da página atual, adicionar o novo membro à lista local sem novo fetch.
  - **Pré:** 1.5 concluída (evento sendo emitido) e `009` concluída (socket já conectado na página).
  - **Aceite:** com dois clientes abertos no mesmo quadro, ao owner adicionar um membro em um cliente, o outro cliente vê o novo membro aparecer na lista sem recarregar a página.
  > ✅ 2026-07-05 22:40 — A `009` já assinava `member.added` no gateway (`use-board-socket.ts`) mas com payload `unknown` e sem handler no estado (comentário `// onMemberAdded: gancho reservado para 010` em `board-view.component.tsx`). Tipado `MemberAddedPayload` (`{ boardId, user: { id, name, email }, role }`) em `use-board-socket.ts` e implementado `onMemberAdded` em `board-view.component.tsx`: acrescenta o membro ao estado `members` (deduplicado por `userId`) sem novo fetch a `GET /members`. O estado `members` vive no `BoardView` (fonte de verdade única) e é passado por props para `BoardToolbar` → `MembersPanel`, que só faz o fetch inicial no `open` do dialog (conforme risco documentado no `design.md`: fetch no mount/abertura, socket só atualiza incrementalmente). Validação com dois clientes de browser reais não foi viável no sandbox (sem navegador headed disponível); validado via leitura do fluxo de dados ponta a ponta (payload do backend → hook tipado → reducer local) e pelo `tsc`/build limpos; comportamento espelha exatamente o padrão já testado manualmente pelo agente de backend na task 1.5 (evento `member.added` confirmado via `socket.io-client` real).

## 3. Verificação

- [x] 3.1 (parte backend) Rodar `npx tsc --noEmit` (backend), os testes unitários e de integração dos casos de uso/endpoints, e validar manualmente com `curl` + socket real o cenário de dois usuários conectados ao mesmo quadro recebendo `member.added` ao vivo.
  - **Aceite:** `tsc` limpo no workspace backend; testes verdes; validação manual do tempo real registrada na evidência.
  > ✅ 2026-07-05 21:56 — `npx tsc --noEmit` limpo em `apps/backend`, `modules/board` e `modules/auth`. `npx jest` verde: `apps/backend` (3 suites/11 testes), `modules/board` (20 suites/92 testes, incluindo os 12 novos de membership), `modules/auth` (4 suites/30 testes). `npx turbo run lint --filter=@taskboard/backend` verde (apenas 1 warning pré-existente em `main.ts`, não relacionado). Validação manual com backend real (`npm run start:dev`, Postgres via `docker-compose`, sem migration nova — schema já cobria `BoardMember`): owner A registra/loga, cria quadro; `POST /boards/:id/members` com e-mail de B → 201; `GET /boards/:id` como B → 200 (B enxerga o quadro); e-mail inexistente → 404 `board.member.not.found`; B (member) tentando `POST` → 403 `board.owner.required`; `GET /members` lista os 2; `DELETE /members/:userId` de B pelo owner → 204. Evento `member.added` confirmado via cliente `socket.io-client` real conectado ao gateway, recebendo `{ boardId, user: { id, name, email }, role: 'member' }` após o `POST` de outro processo. Parte frontend (2.x, painel "Compartilhar") fora do escopo deste agente — não executada.
  - Pendência para o especialista frontend: itens 2.1-2.3 (painel "Compartilhar") e a metade frontend da 3.1 (`npx tsc --noEmit` em `apps/frontend`, já executado aqui e limpo, mas sem UI para validar o consumo dos endpoints/evento).
  > ✅ 2026-07-05 22:45 — (parte frontend) `npx tsc --noEmit -p apps/frontend` limpo. `npx turbo run lint check-types --filter=@taskboard/frontend` verde (apenas 1 warning pré-existente em `app-logo.component.tsx`, não relacionado a esta change). `NEXT_IGNORE_INCORRECT_LOCKFILE=1 npm --workspace @taskboard/frontend run build` verde (rota `/boards/[id]` compilada com sucesso). Sem task `test` configurada no workspace frontend (`turbo run test --filter=@taskboard/frontend` não encontra o script) — nenhum teste automatizado de UI nesta change; validação funcional feita por leitura de código e pelos três comandos acima. Validação com dois usuários/browsers reais não executada por falta de ambiente headed no sandbox (backend não subido nesta sessão); fluxo ponta a ponta (GET/POST/DELETE members + `member.added`) revisado contra os contratos confirmados pela task 1.5/1.6 do backend (payloads e chaves i18n batem com o consumido pelo frontend).
