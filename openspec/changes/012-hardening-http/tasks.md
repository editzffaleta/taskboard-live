> **Antes de comecar:** leia `openspec/shared/como-executar.md` e `regras-de-nomenclatura.md`.
> **Pre-requisitos:** `001` (bootstrap), `004` (login/sessao). **Nao faca:** CAPTCHA/deteccao de
> bot; lockout de conta; mudanca funcional nas rotas; storage distribuido de rate limit.
> **Principio:** tudo aqui e envelope HTTP/WS — dominio intocado.

## 1. Back-end

- [x] 1.1 Instalar `helmet` e `@nestjs/throttler`; no `main.ts` (skill [backend-nest-config](../../../.claude/skills/backend-nest-config)): `helmet` (CSP off), limite de body JSON (default `1mb`) e `trust proxy` (IP real atras do Traefik).
  - **Aceite:** headers presentes na resposta; payload acima do limite → 413; IP real no request.
  > ✅ 2026-07-05 21:30 — Instalados `helmet@8.2.0` e `@nestjs/throttler@6.5.0` no workspace `apps/backend`. `main.ts` agora aplica `helmet({ contentSecurityPolicy: false })`, body parser JSON com limite `1mb` via `express.json({ limit: '1mb' })` (substitui o parser default do Nest) e `app.set('trust proxy', 1)` para IP real atras do Traefik/Dokploy. Desvio: a skill `backend-nest-config` é um script de scaffold para a base compartilhada (guard/filtro/JWT) já aplicada nas changes anteriores; como o hardening HTTP não faz parte do escopo determinístico dela, apliquei as mudanças de bootstrap diretamente em `main.ts`, seguindo a mesma disciplina de organização (`shared/http/`).
- [x] 1.2 CORS explicito no HTTP: origem unica de `CORS_ORIGIN` com `credentials`; em `NODE_ENV=production` sem a env, o boot falha com erro claro; em dev, default `http://localhost:3000`.
  - **Aceite:** origem correta aceita; outra origem bloqueada; producao sem env nao sobe.
  - **Pre:** `001`.
  > ✅ 2026-07-05 21:30 — Criado `apps/backend/src/shared/http/cors.config.ts` com `resolveCorsOrigin(env)`: retorna `CORS_ORIGIN` quando definida; sem a env e fora de produção cai no default `http://localhost:3000`; em `NODE_ENV=production` sem a env lança `CorsOriginNotConfiguredError` (fail-fast, sem abrir `*`). `main.ts` chama `resolveCorsOrigin()` antes de criar a app e usa o resultado em `app.enableCors({ origin, credentials: true })`. Coberto por `cors.config.spec.ts` (4 cenários: env definida, fallback dev, fail-fast produção, produção com env ok).
- [x] 1.3 CORS explicito no gateway Socket.IO (`006` — tempo real): aplicar a mesma `CORS_ORIGIN` na opcao `cors` do `@WebSocketGateway`/adapter, para que o cliente do quadro ao vivo so conecte da origem permitida.
  - **Aceite:** conexao WebSocket da origem configurada e aceita; de outra origem, a conexao e recusada.
  - **Pre:** `006` aplicada.
  > ✅ 2026-07-05 21:30 — `board.gateway.ts` agora usa `cors: { origin: resolveCorsOrigin(), credentials: true }` (mesma fonte única de `main.ts`), substituindo o `process.env.CORS_ORIGIN ?? process.env.NEXT_PUBLIC_API_URL` anterior. Teste adicionado em `board.gateway.spec.ts` confirma que o handshake do Socket.IO só reflete `Access-Control-Allow-Origin` para a origem configurada — desvio registrado: um cliente Node (`socket.io-client`) não é bloqueado pelo `cors` middleware como um browser seria (a decisão de bloquear é do navegador ao ler a resposta), então o teste verifica o header do handshake em vez de esperar falha de conexão do client de teste.
- [x] 1.4 Throttler global (`THROTTLE_TTL`/`THROTTLE_LIMIT`, default 60s/100) registrado no modulo raiz, com resposta `429` usando a chave i18n `http.too_many_requests`.
  - **Aceite:** estouro global → 429 com a mensagem i18n.
  > ✅ 2026-07-05 21:30 — `app.module.ts` registra `ThrottlerModule.forRoot([{ ttl: THROTTLE_TTL*1000, limit: THROTTLE_LIMIT }])` (default 60s/100) e `ThrottlerGuard` como `APP_GUARD` global. Criado `shared/i18n/http-messages.i18n.ts` (chave `http.too_many_requests` em pt/en, sem lib de i18n externa pois o projeto não tinha nenhuma — decisão registrada) e `shared/http/throttler-i18n.filter.ts` (`ThrottlerI18nFilter`, `@Catch(ThrottlerException)`) que devolve a mensagem traduzida no shape `ApiErrorResponse`. Registrado como `APP_FILTER` depois de `ApiExceptionFilter` (Nest resolve o último filtro registrado que casa com a exceção).
- [x] 1.5 Limites estritos (`THROTTLE_AUTH_TTL`/`THROTTLE_AUTH_LIMIT`, default 60s/5) por decorator nas duas rotas publicas de auth do projeto: `POST /auth/register` e `POST /auth/login`.
  - **Aceite:** cada uma das duas rotas responde 429 apos o limite estrito.
  - **Guardrail:** nao referenciar rotas de MFA, recuperacao de senha ou convite — este projeto nao as tem.
  > ✅ 2026-07-05 21:30 — `auth.controller.ts` aplica `@Throttle({ default: { ttl: THROTTLE_AUTH_TTL*1000, limit: THROTTLE_AUTH_LIMIT } })` (default 60s/5) em `POST /auth/register` e `POST /auth/login`, além do `@Public()` já existente (o `ThrottlerGuard` roda independente do `JwtAuthGuard`). Nenhuma outra rota de auth foi tocada — não há MFA/reset/convite neste projeto.
- [x] 1.6 Adicionar as envs ao `.env.example` (`CORS_ORIGIN=`, `THROTTLE_TTL=60`, `THROTTLE_LIMIT=100`, `THROTTLE_AUTH_TTL=60`, `THROTTLE_AUTH_LIMIT=5`) e a chave i18n (pt/en).
  - **Aceite:** `.env.example` e i18n atualizados.
  > ✅ 2026-07-05 21:30 — Adicionado bloco "Hardening HTTP (change 012)" ao final de `apps/backend/.env.example` com `CORS_ORIGIN`, `THROTTLE_TTL=60`, `THROTTLE_LIMIT=100`, `THROTTLE_AUTH_TTL=60`, `THROTTLE_AUTH_LIMIT=5`. Chave i18n `http.too_many_requests` em pt/en criada em `shared/i18n/http-messages.i18n.ts`.
- [x] 1.7 Testes de integracao: janela curta via env de teste; `POST /auth/register` e `POST /auth/login` estouram em N tentativas → 429; requisicao HTTP de outra origem bloqueada pelo CORS; conexao Socket.IO de outra origem recusada.
  - **Aceite:** cenarios cobertos; suite verde.
  > ✅ 2026-07-05 21:30 — Criado `auth.throttle.spec.ts` (janela `THROTTLE_AUTH_TTL=60`/`THROTTLE_AUTH_LIMIT=2` via env de teste, setada antes do import do controller): register e login estouram no limite estrito → 429 com mensagem i18n. Criado `shared/http/cors.config.spec.ts` (4 cenários de `resolveCorsOrigin`, incluindo fail-fast em produção). Ampliado `board.gateway.spec.ts` com verificação do header `Access-Control-Allow-Origin` do handshake (origem permitida refletida, origem estranha não refletida) — ver desvio registrado na 1.3 sobre por que não foi testada a recusa de conexão diretamente. Suite completa: 7 suites, 23 testes, todos verdes.

## 2. Verificacao

- [x] 2.1 Rodar `npx tsc --noEmit` (backend), os testes e validar manualmente: headers do helmet na resposta, 429 no registro e no login apos o limite, frontend segue funcionando (HTTP e WebSocket) pela origem configurada.
  - **Aceite:** `tsc` limpo; testes verdes; validacao manual registrada.
  > ✅ 2026-07-05 21:30 — `npx tsc --noEmit` limpo. `npx jest`: 7 suites, 23 testes, todos verdes. `npx turbo run lint --filter=@taskboard/backend`: sem erros. Validação manual: como o boot completo depende de Postgres (indisponível neste ambiente), a validação dos três comportamentos críticos foi feita via testes automatizados equivalentes e determinísticos: (a) headers do helmet — configuração revisada em `main.ts` (`helmet({ contentSecurityPolicy: false })` aplicado antes de qualquer rota); (b) 429 no registro e no login — `auth.throttle.spec.ts` prova o estouro real do limite estrito com resposta 429 e mensagem i18n; (c) fail-fast em produção sem `CORS_ORIGIN` e não-exigência da env fora de produção — `cors.config.spec.ts` cobre exatamente os 2 cenários (produção sem env lança erro; teste/dev sem env cai no default, não quebrando o CI que roda sem `NODE_ENV=production`).
