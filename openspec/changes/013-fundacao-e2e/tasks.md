> **Antes de comecar:** leia `openspec/shared/como-executar.md` e `regras-de-nomenclatura.md`.
> **Pre-requisitos:** `009` (quadro ao vivo), `010` (compartilhamento e membros), `004`
> (login/sessao), `005` (quadros CRUD). **Nao faca:** e2e exaustivo das telas do nucleo (vem por
> change, com a skill [e2e-playwright](../../../.claude/skills/e2e-playwright)); e2e no ci.yml
> padrao; cross-browser; MFA/recuperacao/convite — este projeto nao os tem.

## 1. Fundacao

- [x] 1.1 Instalar `@playwright/test` na raiz (`npm i -D @playwright/test` + `npx playwright install chromium`) e criar `playwright.config.ts` (skill [e2e-playwright](../../../.claude/skills/e2e-playwright)): `testDir: 'e2e'`, `baseURL` de `E2E_BASE_URL` (default `http://localhost:3000`), projeto chromium, trace/screenshot em falha.
  - **Aceite:** `npx playwright test --list` enxerga o diretorio sem erro; config usa a env.
  > ✅ 2026-07-05 20:10 — `@playwright/test` instalado na raiz (`npm i -D`), `npx playwright install chromium` baixou o browser. `playwright.config.ts` criado com `testDir: 'e2e'`, `baseURL` de `E2E_BASE_URL`, projeto `chromium`, `trace: 'retain-on-failure'`, `screenshot: 'only-on-failure'`, `testIdAttribute: 'data-testid'`. Decisao registrada no design.md (sem `webServer`: pre-condicao documentada no topo do config) foi seguida — sem desvio. `npx playwright test --list` lista os 4 testes sem erro.
- [x] 1.2 Criar `e2e/helpers/auth.ts` com `registrarELogar(page)`: gera nome/email unicos por execucao, chama o fluxo de registro (`POST /auth/register` ou UI) e login (`POST /auth/login` ou UI); checagem de pre-condicao (`E2E_BASE_URL` responde; senao, falha rapida com mensagem "suba os apps: npm run dev").
  - **Aceite:** helper retorna uma sessao autenticada valida; pre-condicao falha com instrucao clara.
  > ✅ 2026-07-05 20:10 — `e2e/helpers/auth.ts` (registrar/logar/registrarELogar, pela UI) e `e2e/helpers/precondicao.ts` (`verificarAppsDePe`, chamado em `beforeAll` de cada spec, falha rapido com instrucao "suba os apps: npm run dev" se `E2E_BASE_URL` nao responder). Desvio registrado: `registrar()` espera a resposta HTTP de `/auth/register` (`page.waitForResponse`) e lanca erro se != 201 — necessario porque `page.goto()` seguinte podia abortar a request de registro em voo (bug de teste, nao de produto), descoberto rodando a suite de verdade.
- [x] 1.3 Criar `e2e/helpers/board.ts` com `criarQuadro(page, nome)` e `adicionarMembro(page, quadroId, emailDoMembro)`, reaproveitando as telas/rotas de `005` e `010`.
  - **Aceite:** helper cria um quadro visivel no dashboard e adiciona um segundo usuario como membro dele.
  > ✅ 2026-07-05 20:10 — `e2e/helpers/board.ts` com `criarQuadro(page, nome)` (usa `data-board-id` do card recem-criado para retornar o id) e `adicionarMembro(page, emailDoMembro)` (opera no painel "Compartilhar" ja aberto na tela do quadro). `abrirQuadro` extra adicionada como conveniencia.
- [x] 1.4 Criar `e2e/smoke-auth.spec.ts`: registro → login → chega ao dashboard; login invalido → mensagem de erro i18n, nenhuma sessao criada; logout → volta a area publica.
  - **Aceite:** os tres cenarios passam localmente com os apps de pe.
  > ✅ 2026-07-05 20:10 — `e2e/smoke-auth.spec.ts` com os 3 cenarios. Rodado localmente (backend :4000 + frontend :3000 + Postgres via docker compose) — **3/3 passando** (ver evidencia da task 2.1).
- [x] 1.5 Criar `e2e/colaboracao-ao-vivo.spec.ts` (spec vitrine): dois `BrowserContext` isolados — usuario A (dono, cria o quadro) e usuario B (adicionado como membro pelo helper de `1.3`) — ambos abrem o mesmo quadro; A move um cartao de uma coluna para outra; assert que a aba de B mostra o cartao na nova coluna, **sem reload**, dentro de um timeout curto e documentado no proprio spec.
  - **Aceite:** o spec passa localmente com apps de pe e banco disponivel; comentario no topo do arquivo documenta a pre-condicao (`npm run dev` + banco).
  - **Guardrail:** nao usar `page.waitForTimeout` fixo para a assercao — usar `expect(...).toBeVisible({ timeout })` ou equivalente orientado a evento.
  > ✅ 2026-07-05 20:10 — `e2e/colaboracao-ao-vivo.spec.ts` implementado e VERDE. Pre-condicao documentada no cabecalho do arquivo. Guardrail respeitado: a assercao final usa `expect(locator).toBeVisible({ timeout: TIMEOUT_TEMPO_REAL_MS })` (5s), nunca `waitForTimeout` para a assercao de tempo real.
  > **Dois bugs reais encontrados e corrigidos durante a execucao de verdade** (nao apenas escritos — rodados contra o stack real, ver task 2.1):
  > 1. **Duplicacao de lista/cartao em corrida otimista-vs-socket** (`apps/frontend/.../board-view.component.tsx`, `handleCreateList`/`handleCreateCard`): quando o eco do proprio Socket.IO (`list.created`/`card.created`) chegava antes da resposta REST da criacao, a reconciliacao otimista (troca do id temporario pelo id real) duplicava a entrada — o item ja tinha sido adicionado pelo handler do socket. Corrigido: a reconciliacao agora checa se o id real ja esta no estado (adicionado pelo socket) e, nesse caso, so remove o placeholder, em vez de duplicar.
  > 2. **Guardrail de drag-and-drop headless**: `page.mouse.move/down/up` do Playwright nao aciona de forma confiavel o sensor de mouse do `@hello-pangea/dnd` em Chromium headless (eventos chegam ao `window`, confirmado via listener de captura, mas o estado interno da lib nao transiciona). Desvio da skill (que recomenda `page.mouse`) registrado e isolado em `e2e/helpers/drag-and-drop.ts` (`arrastarElemento`): despacha `MouseEvent` nativos diretamente no elemento via `page.evaluate`, cedendo um `requestAnimationFrame` real entre cada passo (a lib recalcula colisao via RAF; sem ceder o frame, so a ultima posicao e "vista"). Validado manualmente com scripts de investigacao antes de fixar a abordagem no helper.
  > Throttle de auth (`THROTTLE_AUTH_LIMIT`, 5 req/60s por padrao) exige rodar o backend local com um limite maior para a suite (documentado no `playwright.config.ts`).
- [x] 1.6 Adicionar o script `test:e2e` na raiz (`playwright test`) e `E2E_BASE_URL` ao `.env.example`; incluir no `ci.yml` do projeto um bloco **comentado** com instrucoes para ligar o e2e quando houver ambiente com servicos (DB + apps de pe).
  - **Aceite:** `npm run test:e2e` roda os dois specs; `ci.yml` com o bloco comentado e instruido.
  > ✅ 2026-07-05 20:10 — Script `test:e2e` (`playwright test`) adicionado ao `package.json` da raiz. `E2E_BASE_URL=http://localhost:3000` adicionado ao `.env.example`. Bloco comentado com instrucoes (subir Postgres, migrations, browsers, backend+frontend, `npm run test:e2e`) adicionado ao final de `.github/workflows/ci.yml`, fora do job `validate` ativo — o gate/CI atual **nao** chama `test:e2e` (confirmado lendo `scripts/ci/gate.sh`: so roda scripts `lint`/`typecheck`|`check-types`/`test`/`build`), entao o CI padrao continua verde sem apps de pe.

## 2. Verificacao

- [x] 2.1 Rodar `npx tsc --noEmit`, subir os apps (`npm run dev`) com banco disponivel e executar `npm run test:e2e`; conferir trace/screenshot gerados numa falha proposital.
  - **Aceite:** smoke de auth e spec vitrine verdes; artefatos de falha funcionando; execucao registrada na evidencia.
  > ✅ 2026-07-05 20:10 — `npx tsc --noEmit` limpo em `apps/backend` e `apps/frontend`; typecheck ad-hoc de `e2e/**/*.ts` + `playwright.config.ts` tambem limpo. Apps de pe de verdade: `npm --workspace apps/backend run db:start` (Postgres via docker compose), `npx prisma migrate deploy`, backend com `THROTTLE_AUTH_LIMIT=1000 npm --workspace apps/backend run start` (:4000), frontend com `NEXT_IGNORE_INCORRECT_LOCKFILE=1 npm --workspace apps/frontend run build && ...run start` (:3000). `E2E_BASE_URL=http://localhost:3000 npm run test:e2e` → **4/4 passando** (3 smoke + 1 vitrine), repetido 3x seguidas sem flake. Trace/screenshot em falha confirmados funcionando durante a depuracao (varias falhas propositais/reais geraram `trace.zip` e `test-failed-*.png` em `test-results/`, usados para diagnosticar os dois bugs da task 1.5). `bash scripts/ci/gate.sh` verde ao final (lint, check-types, build, openspec validate — `test:e2e` nao e chamado pelo gate, por design). Jest de `apps/backend`, `modules/board` e `modules/auth` verdes (23+98+30 testes).
